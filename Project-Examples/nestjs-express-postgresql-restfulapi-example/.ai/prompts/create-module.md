# Create Module

Create a new module: {{moduleName}}

Requirements:

- Follow folder structure rules
- Include:
  - admin controller
  - public controller
  - service
  - repository
  - entity
  - DTO (request + response)

- Admin API: full CRUD
- Public API: read only

- Use BaseQueryDto for listing

Return full code structure.